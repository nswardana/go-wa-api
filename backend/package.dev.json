// Development-only package.json scripts
{
  "scripts": {
    "dev": "nodemon src/app.js --exec 'npm run lint && node src/app.js'",
    "dev:fast": "nodemon src/app.js",
    "start": "node src/app.js",
    "lint": "eslint src/",
    "test": "jest",
    "migrate": "node scripts/migrate.js"
  },
  "devDependencies": {
    "nodemon": "^3.0.2",
    "eslint": "^8.57.0",
    "jest": "^29.7.0"
  }
}
